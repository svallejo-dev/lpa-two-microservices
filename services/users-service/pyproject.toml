[project]
name = "users-service"
version = "1.0.0"
description = "Microservicio de Usuarios - Taller LPA2"
requires-python = ">=3.11"

[tool.pytest.ini_options]
# `auto` evita decorar cada prueba async con @pytest.mark.asyncio.
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q"
